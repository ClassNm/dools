const mutations = {
  // 页面级存储、
    // SET_APP_NAME(state,params){
    //     state.appName = params
    // },
    // GET_APP_NAME(state,params){
    //     state.SerialNumber = params;
    // }

    // 改变state里的isLogin里的值 进行判断 是否需要显示相对应的页面(mock,statement...)
    // changeLogin(state,data){
    //     state.isLogin = data;
    //   }
};
export default mutations